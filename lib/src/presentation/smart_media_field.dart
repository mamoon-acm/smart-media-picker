import 'package:flutter/material.dart';

/// Main widget for picking and displaying media.
class SmartMediaField extends StatelessWidget {
  const SmartMediaField({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
